简单工厂模式就是构造一个单一的工厂，在工厂中进行判断，从而生产不同的产品。
如例子中所示，要设计一个简单的计算器以实现加、减、乘、除的运算，我们可以首先创造一个运算基类，
从而派生出加法类、减法类、乘法类和除法类，我们还需要一个工厂类，根据不同的输入符号，产生不同的产品（即不同的运算类），运用虚函数的多态性进行加减乘除计算。

当然，简单工厂类也有缺陷，如果我们需要增加产品（如开方sqrt运算），我们就要修改工厂类，这就违反了封装原则（可扩展，不可修改）。
